//arguments in node js
//passing the argument from console
const stName= process.argv[2] || 'Jhon'; //default argumnet would be jhon
const stName2= process.argv[3] || 'Doe'; //default argumnet would be doe ;
console.log(`Hello ${stName} ${stName2}`);  
//code to run node server in same way
